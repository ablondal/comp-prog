#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
using namespace std;
#define max(a,b) ((a>b)?a:b)
#define min(a,b) ((a<b)?a:b)
#define fori(i,k) for(int i=0; i<k; ++i)

struct{
	int row;
	int col;
}keytab[26]

int main(){
	int n;
	while(cin >> n){
		string key;
		cin >> key;
		key.toLower();
	}
}