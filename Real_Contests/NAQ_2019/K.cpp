#include <iostream>
#include <string>
using namespace std;


int main(){
    string events;
    cin >> events;
    int next[events.size()];
    int last[events.size()];
    int l[26];
    for(int i=0; i< events.size())
}